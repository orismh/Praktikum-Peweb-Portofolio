Penjelasan Kode

  HTML

Basic

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portofolio Saya</title>

Penghubung CSS
<link rel="stylesheet" href="CTajam.css">
</head>
<body>

**Header dan Navigasi**
<header>
        <div class="logo">Portofolio Moment<span>.</span></div>
        <nav id="navbar">
            <ul>
                <li><a href="#hero">Beranda</a></li>
                <li><a href="#about">Tentang</a></li>
                <li><a href="#pengalaman">Pengalaman</a></li>
                <li><a href="#contact">Kontak</a></li>
            </ul>
        </nav>
        <div class="menu-toggle" id="menu-toggle">&#9776;</div>
    </header>



Main Section

<section id="hero" class="hero">
        <div class="hero-content">
            <img src="Foto Profil.jpeg"" alt="Foto Profil Saya" class="profile-img">
            <h1>Halo, Saya RizkyMH <span>Seorang Pemula</span></h1>
            <p>Website ini untuk kebutuhan Tugas Praaktikum Matkul Pengembangan Web</p>
            <a href="#pengalaman" class="btn">Lihat Karya</a>
        </div>
    </section>

Section Tentang Saya
<section id="about" class="about">
        <h2>Tentang Saya</h2>
        <p>Saya seorang mahasiswa Universitas Jember semeste 3 dan saya merupakan seorang Pemula dalam pengembangan Web</p>
    </section>




**Section Pengalaman**
 <section id="pengalaman" class="pengalaman">
        <h2>Pengalaman Saya</h2>
        <div class="project-grid">
            <div class="project-card">
                <h3>Proyek 1</h3>
                <p>Berpartisipasi dalam pengembangan Game Roblox sebagai Environmental Builder</p>
            </div>
            <div class="project-card">
                <h3>Proyek 2</h3>
                <p>Membuat web List belanja</p>
            </div>
            <div class="project-card">
                <h3>Proyek 3</h3>
                <p>None</p>
            </div>
        </div>
    </section>



Section Kontak

<section id="contact" class="contact">
        <h2>Hubungi Saya</h2>
        <p class="contact-subtitle">Silakan isi formulir di bawah ini untuk mengirim pesan.</p>
        
        <form class="contact-form" id="contactForm">
            <div class="form-group">
                <label for="fullName">Full Name</label>
                <input type="text" id="fullName" placeholder="Full Name" required>
            </div>

            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" id="email" placeholder="nama@gmail.com" required>
            </div>

            <div class="form-group">
                <label for="subject">Subject</label>
                <input type="text" id="subject" placeholder="Subject yang ingin di kirim" required>
            </div>

            <div class="form-group">
                <label for="message">Message</label>
                <textarea id="message" rows="5" placeholder="Pesan..." required></textarea>
            </div>

            <button type="submit" class="btn-send">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                </svg>
            Send Message
            </button>
        </form>
    </section>
    <footer>
        <p>&copy; 2026 Portofolio Saya. All rights reserved.</p>
    </footer>
    



Penghubung JS

<script src="JavaJS.js"></script>
</body>
</html>


  **CSS**



**Dasar**
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #0f172a;
    color: #f8fafc;
    line-height: 1.6;
}



Header dan navigasi

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 8%;
    background-color: #1e293b;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.logo {
    font-size: 1.5rem;
    font-weight: bold;
}

.logo span {
    color: #38bdf8;
}

nav ul {
    display: flex;
    list-style: none;
    gap: 20px;
}

nav a {
    color: #f8fafc;
    text-decoration: none;
    transition: color 0.3s;
}

nav a:hover {
    color: #38bdf8;
}

.menu-toggle {
    display: none;
    font-size: 1.5rem;
    cursor: pointer;
}

/* Section Common Styles */
section {
    padding: 80px 8%;
    text-align: center;
}



Hero Section

.hero {
    min-height: 80vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(135deg, #0f172a, #1e293b);
}

.hero h1 {
    font-size: 2.5rem;
    margin-bottom: 15px;
}

.hero span {
    color: #38bdf8;
}

.hero p {
    margin-bottom: 25px;
    color: #94a3b8;
}

.btn {
    display: inline-block;
    padding: 10px 25px;
    background-color: #38bdf8;
    color: #0f172a;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    transition: background 0.3s;
}

.btn:hover {
    background-color: #0284c7;
    color: #fff;
}



Section Pengalaman & Footer

.project-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-top: 30px;
}

.project-card {
    background-color: #1e293b;
    padding: 20px;
    border-radius: 8px;
    border: 1px solid #334155;
    text-align: left;
}

.project-card h3 {
    margin-bottom: 10px;
    color: #38bdf8;
}

.project-card p {
    color: #94a3b8;
}

footer {
    text-align: center;
    padding: 20px;
    background-color: #1e293b;
    color: #64748b;
    font-size: 0.9rem;
}



Responsif Mobile

@media (max-width: 768px) {
    .menu-toggle {
        display: block;
    }

    nav {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background-color: #1e293b;
        padding: 20px 0;
    }

    nav.active {
        display: block;
    }

    nav ul {
        flex-direction: column;
        align-items: center;
    }
}


Formulir Kontak

.contact-subtitle {
    color: #94a3b8;
    margin-bottom: 30px;
}

.contact-form {
    max-width: 600px;
    margin: 0 auto;
    background-color: #ffffff;
    padding: 30px;
    border-radius: 16px;
    text-align: left;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
}

.form-group {
    margin-bottom: 20px;
}

.form-group label {
    display: block;
    font-size: 0.9rem;
    font-weight: 600;
    color: #1e293b;
    margin-bottom: 8px;
}

.form-group input,
.form-group textarea {
    width: 100%;
    padding: 12px 16px;
    border: 1px solid #e2e8f0;
    background-color: #f8fafc;
    border-radius: 10px;
    font-size: 0.95rem;
    color: #334155;
    outline: none;
    transition: all 0.2s ease-in-out;
}

.form-group input::placeholder,
.form-group textarea::placeholder {
    color: #94a3b8;
}

.form-group input:focus,
.form-group textarea:focus {
    background-color: #ffffff;
    border-color: #8b5cf6;
    box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.15);
}

.btn-send {
    width: 100%;
    padding: 14px;
    background-color: #4fa6d8;
    color: #ffffff;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
    transition: background-color 0.2s ease-in-out, transform 0.1s;
}

.btn-send:hover {
    background-color: #7c3aed;
}

.btn-send:active {
    transform: scale(0.99);
}

/* Profil Foto */
.hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
}

.profile-img {
    width: 150px;              || Lebar foto
    height: 150px;             || Tinggi foto
    object-fit: cover;         || Agar foto tidak memanjang/gepeng
    border-radius: 50%;        || Membuat foto berbentuk lingkaran sempurna
    border: 4px solid #38bdf8; || Garis tepi berwarna biru terang
    box-shadow: 0 0 20px rgba(56, 189, 248, 0.4); || Efek Border Foto
    margin-bottom: 20px;       || Jarak foto ke teks di bawahnya
    transition: transform 0.3s ease-in-out;
}

.profile-img:hover {
    transform: scale(1.05);    || Efek Hover
}


JS

Versi Mobile
const menuToggle = document.getElementById('menu-toggle');
const navbar = document.getElementById('navbar');

if (menuToggle && navbar) {
    menuToggle.addEventListener('click', () => {
        navbar.classList.toggle('active');
    });
}

Penutup Menu Otomatis
const navLinks = document.querySelectorAll('nav a');
navLinks.forEach(link => {
    link.addEventListener('click', () => {
        if (navbar.classList.contains('active')) {
            navbar.classList.remove('active');
        }
    });
});

Pengiriman Formulir

const contactForm = document.getElementById('contactForm');
if (contactForm) {
    contactForm.addEventListener('submit', (e) => {
        e.preventDefault();
        alert('Pesan Terkirim');
        contactForm.reset();
    });
}

document.addEventListener('DOMContentLoaded', () => {

    // 1. NAVIGASI MOBILE (TOGGLE & AUTO CLOSE)
    const menuToggle = document.getElementById('menu-toggle');
    const navbar = document.getElementById('navbar');
    const navLinks = document.querySelectorAll('nav ul li a');

    if (menuToggle && navbar) {
        menuToggle.addEventListener('click', () => {
            navbar.classList.toggle('active');
        });
    }

Menutup menu mobile secara otomatis waktu link diklik

navLinks.forEach(link => {
        link.addEventListener('click', () => {
            navbar.classList.remove('active');
        });
    });

    HIGHLIGHT NAVIGASI & HEADER SCROLL EFFECT

    const sections = document.querySelectorAll('section');
    const header = document.querySelector('header');

    window.addEventListener('scroll', () => {
        let currentSection = '';

        // Efek bayangan pada header saat di-scroll
        if (window.scrollY > 50) {
            header.classList.add('header-scrolled');
        } else {
            header.classList.remove('header-scrolled');
        }

        Mendeteksi posisi section aktif saat ini

        sections.forEach(section => {
            const sectionTop = section.offsetTop - 120;
            const sectionHeight = section.clientHeight;
            if (window.scrollY >= sectionTop && window.scrollY < sectionTop + sectionHeight) {
                currentSection = section.getAttribute('id');
            }
        });

        // Memberikan class 'active' pada link navigasi yang sesuai
        navLinks.forEach(link => {
            link.classList.remove('active-link');
            if (link.getAttribute('href') === `#${currentSection}`) {
                link.classList.add('active-link');
            }
        });
    });

Efek Teks Ketik otomatis di beranda


 const heroSpan = document.querySelector('.hero h1 span');
    if (heroSpan) {
        const roles = ['Seorang Pemula', 'Mahasiswa UNEJ', 'Game Enthusiast'];
        let roleIndex = 0;
        let charIndex = 0;
        let isDeleting = false;

        function typeEffect() {
            const currentRole = roles[roleIndex];

            if (isDeleting) {
                heroSpan.textContent = currentRole.substring(0, charIndex - 1);
                charIndex--;
            } else {
                heroSpan.textContent = currentRole.substring(0, charIndex + 1);
                charIndex++;
            }

            let typeSpeed = isDeleting ? 50 : 100;

            if (!isDeleting && charIndex === currentRole.length) {
                typeSpeed = 2000; // Tahan kata selama 2 detik
                isDeleting = true;
            } else if (isDeleting && charIndex === 0) {
                isDeleting = false;
                roleIndex = (roleIndex + 1) % roles.length;
                typeSpeed = 500;
            }

            setTimeout(typeEffect, typeSpeed);
        }

        typeEffect();
    }

Scroll Reveal

const observerOptions = {
        threshold: 0.15
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('reveal-show');
            }
        });
    }, observerOptions);

    const revealElements = document.querySelectorAll('.project-card, .about, .contact-form');
    revealElements.forEach(el => {
        el.classList.add('reveal-hidden');
        observer.observe(el);
    });


Form Kontak

const contactForm = document.getElementById('contactForm');
    if (contactForm) {
        contactForm.addEventListener('submit', (e) => {
            e.preventDefault();

            const nameInput = document.getElementById('fullName').value;

            // Tampilkan notifikasi toast interaktif
            showToast(`Terima kasih ${nameInput}, pesan Anda berhasil terkirim!`);
            contactForm.reset();
        });
    }

Fungsi membuat elemen DOM Toast secara dinamis

function showToast(message) {
        const toast = document.createElement('div');
        toast.className = 'custom-toast';
        toast.innerText = message;
        document.body.appendChild(toast);

        setTimeout(() => toast.classList.add('show'), 100);

        setTimeout(() => {
            toast.classList.remove('show');
            setTimeout(() => toast.remove(), 300);
        }, 3500);
    }
});